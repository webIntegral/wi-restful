/**
 * wi.resource.currentUser
 * 
 * Module Current User Resource
 * 
 * Resource to get current user from server.
 * Server uses Satellizer's Acces Token to find user.
 * 
 */
(function() {
	'use strict';
	
	angular
		.module('wi.resource.currentUser', [
            'ngResource'
        ])
})();